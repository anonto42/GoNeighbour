import { Router } from "express";



const router = Router();


router
    .route("/")
    .post(
        
    )

router
    .route("/connection")
    .post(
        
    )

router
    .route("/connection")
    .post(
        
    )

router
    .route("/payment")
    .post(
        
    )

router
    .route("/success")
    .get(
        
    )

router
    .route("/cancel")
    .get(
        
    )


export const PaymentRouter = router;